name: Mi 11 Ultra Kernel Hybrid - KSU/SuSFS/KPM

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout kernel source
        uses: actions/checkout@v4
        with:
          repository: joel27/android_kernel_xiaomi_sm8350
          ref: lineage-23.2-ksu
          fetch-depth: 1

      - name: Install dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y \
            git bc bison flex libssl-dev \
            make libelf-dev python3 \
            zip curl wget cpio \
            clang-12 lld-12 llvm-12

      - name: Setup Clang 12 toolchain
        run: |
          mkdir -p clang/bin
          for tool in clang clang++ ld.lld llvm-ar llvm-nm \
                      llvm-objcopy llvm-objdump llvm-strip; do
            ln -sf /usr/bin/${tool}-12 clang/bin/${tool}
          done
          clang/bin/clang --version

      - name: Setup GCC toolchain
        run: |
          git clone --depth=1 \
            https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_aarch64_aarch64-linux-android-4.9 \
            gcc64
          git clone --depth=1 \
            https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_arm_arm-linux-androideabi-4.9 \
            gcc32

      - name: Add KernelSU / SukiSU base
        run: |
          curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s main
          echo "Root framework añadido"

      - name: Add Xiaomi missing symbols stubs
        run: |
          mkdir -p drivers/soc/qcom
          cat > drivers/soc/qcom/xiaomi_stubs.c << 'EOF'
          #include <linux/kernel.h>
          #include <linux/export.h>

          int get_hw_version_platform(void) { return 0; }
          EXPORT_SYMBOL(get_hw_version_platform);

          int get_hw_country_version(void) { return 0; }
          EXPORT_SYMBOL(get_hw_country_version);

          int get_hw_version_build(void) { return 0; }
          EXPORT_SYMBOL(get_hw_version_build);

          int get_hw_version_major(void) { return 0; }
          EXPORT_SYMBOL(get_hw_version_major);

          int get_hw_version_minor(void) { return 0; }
          EXPORT_SYMBOL(get_hw_version_minor);

          int mi_disp_register_client(void *data) { return 0; }
          EXPORT_SYMBOL(mi_disp_register_client);

          int mi_disp_unregister_client(void *data) { return 0; }
          EXPORT_SYMBOL(mi_disp_unregister_client);
          EOF

          if ! grep -q "xiaomi_stubs.o" drivers/soc/qcom/Makefile; then
            echo 'obj-y += xiaomi_stubs.o' >> drivers/soc/qcom/Makefile
          fi

      - name: Configure kernel
        run: |
          cp arch/arm64/configs/vendor/lahaina-qgki_defconfig arch/arm64/configs/star_ksu_defconfig

          cat >> arch/arm64/configs/star_ksu_defconfig << 'EOF'

          CONFIG_CMDLINE="cgroup_disable=pressure ramoops.mem_address=0xA0000000 ramoops.mem_size=0x100000 ramoops.console_size=0x40000 ramoops.record_size=0x40000"
          CONFIG_CMDLINE_EXTEND=y

          CONFIG_STACKPROTECTOR=y
          CONFIG_STACKPROTECTOR_STRONG=y
          CONFIG_PSTORE=y
          CONFIG_PSTORE_RAM=y
          CONFIG_PSTORE_CONSOLE=y
          CONFIG_PSTORE_PMSG=y

          CONFIG_KSU=y
          CONFIG_KPM=y

          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y
          CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
          CONFIG_KSU_SUSFS_SUS_MAP=y

          CONFIG_BUILD_ARM64_UNCOMPRESSED_KERNEL=y
          CONFIG_BUILD_ARM64_DT_OVERLAY=y
          # CONFIG_BUILD_ARM64_APPENDED_DTB_IMAGE is not set
          EOF

          if [ -f arch/arm64/configs/vendor/star_QGKI.config ]; then
            cat arch/arm64/configs/vendor/star_QGKI.config >> arch/arm64/configs/star_ksu_defconfig
          fi

      - name: Build kernel
        run: |
          export PATH="$GITHUB_WORKSPACE/clang/bin:$GITHUB_WORKSPACE/gcc64/bin:$GITHUB_WORKSPACE/gcc32/bin:$PATH"

          make O=out ARCH=arm64 \
            CC=clang \
            LD=ld.lld \
            AR=llvm-ar \
            NM=llvm-nm \
            OBJCOPY=llvm-objcopy \
            OBJDUMP=llvm-objdump \
            STRIP=llvm-strip \
            CROSS_COMPILE=aarch64-linux-android- \
            CROSS_COMPILE_ARM32=arm-linux-androideabi- \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            star_ksu_defconfig

          make -j$(nproc --all) O=out ARCH=arm64 \
            CC=clang \
            LD=ld.lld \
            AR=llvm-ar \
            NM=llvm-nm \
            OBJCOPY=llvm-objcopy \
            OBJDUMP=llvm-objdump \
            STRIP=llvm-strip \
            CROSS_COMPILE=aarch64-linux-android- \
            CROSS_COMPILE_ARM32=arm-linux-androideabi- \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            2>&1 | tee build.log

      - name: Package with AnyKernel3
        run: |
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3 AnyKernel3

          if [ -f out/arch/arm64/boot/Image ]; then
            cp out/arch/arm64/boot/Image AnyKernel3/
          elif [ -f out/arch/arm64/boot/Image.gz ]; then
            cp out/arch/arm64/boot/Image.gz AnyKernel3/
          elif [ -f out/arch/arm64/boot/Image.gz-dtb ]; then
            cp out/arch/arm64/boot/Image.gz-dtb AnyKernel3/
          else
            echo "❌ No image found"
            ls out/arch/arm64/boot/ || true
            exit 1
          fi

          if [ -f out/arch/arm64/boot/dtbo.img ]; then
            cp out/arch/arm64/boot/dtbo.img AnyKernel3/
          fi

          sed -i 's|do.devicecheck=1|do.devicecheck=0|g' AnyKernel3/anykernel.sh
          grep -q "^BLOCK=" AnyKernel3/anykernel.sh && sed -i 's|^BLOCK=.*|BLOCK=boot;|g' AnyKernel3/anykernel.sh || echo 'BLOCK=boot;' >> AnyKernel3/anykernel.sh
          grep -q "IS_SLOT_DEVICE=" AnyKernel3/anykernel.sh && sed -i 's|IS_SLOT_DEVICE=.*|IS_SLOT_DEVICE=1;|g' AnyKernel3/anykernel.sh || echo 'IS_SLOT_DEVICE=1;' >> AnyKernel3/anykernel.sh
          grep -q "device.name1=" AnyKernel3/anykernel.sh && sed -i 's|device.name1=.*|device.name1=star|g' AnyKernel3/anykernel.sh || true

          cd AnyKernel3
          zip -r9 ../Mi11Ultra-Hybrid-$(date +%Y%m%d-%H%M).zip . -x ".git*" -x "*.md"

      - name: Upload kernel zip
        uses: actions/upload-artifact@v4
        with:
          name: Mi11Ultra-Hybrid
          path: Mi11Ultra-Hybrid-*.zip
          retention-days: 7

      - name: Upload build log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build.log
